# G0 基线证据

日期：2026-08-23（Asia/Tokyo）

## 仓库状态

- 分支：`codex-build-sim-upgrade`
- 远程：`origin git@github.com:Mankindevil/Build-sim-.git`
- 基线时工作区：干净

## 命令

| 命令 | 结果 |
| --- | --- |
| `npm test` | 通过，15 个文件 / 186 个测试 |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过，Vite 产物生成 |
| `node tests/legacy-run-model-tests.js` | 通过，85 个断言、0 failures |
| `node tests/legacy-run-static-tests.js` | 通过，23 个断言、0 failures |

legacy runner 原先因仓库为 ESM 而使用 CommonJS `require`，并且仍引用已归档的预览路径；G0 将入口改为 ESM，并明确指向 `legacy/v1`。

## G0 护栏

- `BUILD_SIM_CATALOG_WRITE_ENABLED` 与 `BUILD_SIM_ADVICE_ENABLED` 默认 `false`。
- 价格/候选 JSON 写入使用临时文件后原子 `rename`。
- 已存在旧值先复制到 `data/audit/rollback/`，并在 rollback manifest 记录旧 hash、新 hash、目标和备份引用。
- `tests/fixtures/upgrade-scenarios/` 固定离线基线、双 PSU、240 冷排、9 HDD/HBA、缺字段、重复 MPN、价格 snapshot 与 DeepSeek disabled 场景。

